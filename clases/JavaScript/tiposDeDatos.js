//Inicializamos un dato tipo entero (Number)
let edad = 25;
//Inicializamos dato tipo decimal 
let precio = 25.000;

//Inicializamos dato tipo texto (String)
let nombre = "Luisa";
//Se pueden usar comillas dobles o simples
let mensaje = 'Hola, Bienvenidos al bootcamp JAVA';

//Inicializamos un dato tipo arreglo
let esMayorDeEdad = true;
let tieneDescuento = false;

//Inicializamos un dato tipo arreglo
let colores = ["yellow", "green", "red"]; //corchetes

//Inicializamos un dato tipo objeto
let objeto = {
    nombre: "Ana",
    edad: 30,
    casada: false
}; //llaves

//Inicializar un dato tipo nulo y no definido
let valorNulo = null;
let valotNoDefinido;

//Inicializar un dato constante
const pi = 3.1416;
